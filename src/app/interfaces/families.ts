export interface Family {
    id?: string;
    familyName: string;
    phone: string;
    email: string;
    familyMembers: number;
    flatNumber: string;
    createdAt: string;
    isActive: boolean;
}